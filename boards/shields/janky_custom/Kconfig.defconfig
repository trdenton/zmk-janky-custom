if SHIELD_JANKY_CUSTOM_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Jankmaster666"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_JANKY_CUSTOM_LEFT || SHIELD_JANKY_CUSTOM_RIGHT

config ZMK_SPLIT
    default y

endif
